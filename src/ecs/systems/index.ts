import SpriteSystem from './SpriteSystem';
import SendTextSystem from './SendTextSystem';

enum RunType {
  'tick' = 'tick',
  'trigger' = 'trigger',
}

export { RunType, SpriteSystem, SendTextSystem };

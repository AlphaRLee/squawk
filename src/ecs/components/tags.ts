export enum Tags {
  'new' = 'new',
  'interactiveSprite' = 'interactiveSprite',
  'gravity' = 'gravity',
  'ground' = 'ground',
  'destroyRequested' = 'destroyRequested',
  'destroyed' = 'destroyed',
}

// export default Tags;
